# Clappy build status

Updated September 7 evening (September 8 UTC), 2026. The hosted virtual-camera prototype works; this is not a finished submission or frame-accurate/native-camera acceptance.

## Current direction

Root `IMPLEMENTATION_PLAN.md` revision 2 is authoritative. iPhone-only target, three virtual phones for the visual demo, no required physical shoot. Adopt MediaMTX + FFmpeg + OTIO; OBS optional; native HaishinKit evaluation later. This Codex owns development; Google Cloud AI powers the app.

## Verified state

- Google Cloud project `clappy-cinema-2026-0907`, number `132530031832`, ACTIVE. Billing enabled with user approval. Dedicated hosted VM now running; the unrelated default gcloud project remains untouched. See hosting evidence below.
- Real `gemini-2.5-flash` global Vertex request succeeded. Expired local ADC is not silently reused: development explicitly uses the existing signed-in gcloud identity. Deployment must use ADC / attached service account.
- React/Vite/TypeScript production setup, three virtual iPhone cards, real WebRTC program monitor, manual arm/roll/cut/switch/hold, review, OTIO and MP4 downloads.
- Three supervised FFmpeg publishers → pinned MediaMTX → actual browser video and separately recorded fMP4 files. Owned 60-second frame-counter/click-track sources with SHA-256 manifests.
- SQLite/WAL state, revisioned commands, deduplication, session-scoped tokens, event outbox and truthful restart interruption states.
- OTIO compiler / FFmpeg renders from preserved fixture originals with continuous master audio and validated source ranges. Tests verify frame identities across cuts and unchanged originals.
- Real API media smoke passed after stop-path improvement: three verified recordings, 6.067-second C/A/B edit. Evidence: `artifacts/media-smoke.json`.
- Official ClickHouse MCP `run_query` discovered and exercised. Event redelivery deduplicated; other-production data and base-table reads are denied, as are writes. `scripts/smoke_memory.py` passes.
- Google ADK recalled a saved Bella/letter direction after restarting agent state, using real MCP. One request took 2.96 seconds. Evidence: `artifacts/director-smoke.json`.
- Full browser → API → Gemini/ClickHouse → OTIO → rendered alternate MP4 passed. The original edit stayed available; alternate Bella screen time met the requested minimum 75%. Both browser tests passed in 16.6 seconds. Screenshot: `artifacts/studio-ai-edit.png`.
- An earlier AI test caught a 73.5% allocation instead of 75%. The coordinator now validates exact frame minima and permits one bounded model correction before rejecting invalid output.
- Eight Python tests pass; production web build passes. Combined Chrome media/AI edit/mobile/voice suite: three passed in 34.3 seconds. WebKit (iPhone 13 emulation) real WebRTC/MP4 and layout tests: two passed in 19.2 seconds. These are browser-engine tests, not physical iPhone validation.
- Both Python environments and npm are locked. ADK uses MCP 1.x; official mcp-clickhouse 0.6 uses MCP 2.x in its own environment under `infra/mcp-clickhouse`, connected over stdio.
- Created six lines of original two-character Google Cloud Text-to-Speech dialogue. Runtime camera manifests exclude the renderer's evaluation timing. Pictures remain test charts, now optionally with dialogue audio.
- Actual realtime-paced audio → Google Speech-to-Text → script alignment recognized all six lines. Evidence: `artifacts/speech-smoke.json`.
- Real RTSP performance audio → Google speech → Google ADK + ClickHouse MCP live direction passed: six recognized lines, five memory-backed decisions, manual hold/release precedence, 38.733-second take. Evidence: `artifacts/live-director-smoke.json`.
- Separate director-audio API passed spoken roll, hold Bella, go wide and cut, including deduplication. After-upload completion: 2.039 / 1.862 / 1.714 / 3.635 seconds respectively; cut includes recording finalization. Evidence: `artifacts/voice-smoke.json`.
- Browser MediaRecorder with synthetic microphone → Google speech → actual camera roll passed (11.6 seconds for the complete test including capture and cut/review). Screenshot: `artifacts/studio-voice-roll.png`. Mic only opens on user action, caps capture at 10 seconds, releases tracks, and uploads to Google. No physical microphone validation claimed.
- Spoken next-line queue passed: target line 0, real transcription confirmed completion, manual hold deferred the switch, release applied wide. Evidence: `artifacts/queued-voice-smoke.json`. An initial ASR error (“white” versus “wide”) was corrected with Google speech vocabulary hints; ambiguous intent stays rejected and voice failures retain diagnostic evidence.
- Original scene plate generated with Google `gemini-3.1-flash-image` (global). The older Imagen 4 model returned 404; no image was fabricated or substituted from another AI provider. Asset/prompt/hash provenance: `assets/demo/last-train-v1/`. Three immutable 60-second animatic sources rendered separately under `data/scenes/last-train-animatic-v1`; derived crop and still-frame/voice disclosures are burned into footage.
- Open live-action demo source added from Pexels under its published license: one 4096×2160/25 fps, 40.8-second continuous café take by cottonbro studio. `scripts.prepare_open_footage` deterministically creates synchronized Tom, Bella, and wide crops at 960×540/30 fps, extends the post-dialogue tail to the 60-second rehearsal clock, and pairs each with the existing Google-voiced fictional dialogue. Source URL, license URL and SHA-256 are recorded in `assets/demo/open-cafe-v1/provenance.json`; the views are disclosed as derived, not physical cameras or lip-synced performances.
- Local end-to-end verification completed on the open-footage source: all 1,800 frames in each derived camera passed the visible frame-code integrity check, the media hub streamed all three views, Google speech tracking observed the shared production audio, and the 20.733-second live cut rendered from three independently recorded streams.
- Cinematic UI flow verified: authenticated previews decoded at 960 px, actual WebRTC animatic playback, Google recognition of lines 0–4 during a 29.5-second take, playable MP4, and a real Gemini/MCP alternate with 93.3% Bella allocation. Session `93d343d46b954b11a7833f8989e11eca`; take `deae926e2f114b8a966b62295be88546`. Original and alternate retained. Screenshots: `artifacts/studio-animatic-live.png` and `artifacts/studio-animatic-alternate.png`.
- Master monitor audio verified: only the program's camera C player unmuted; the other five players muted. A/B switching preserved a paused playhead at exactly 7 seconds; a second switch preserved active playback (8.455 seconds when inspected after the click).
- Camera watchdog now observes actual process and MediaMTX path health, marks interrupted cameras OFFLINE, falls back from an unavailable selected camera, and invalidates stale agent proposals. Dead cameras cannot receive manual or AI cuts. Recording verification checks duration and observed interruptions, not just presence of a decodable fragment.
- Abrupt publisher-crash test passed: held camera A failed, program fell back to C, hold released, dead-camera switch rejected, and final coverage was A partial / B and C complete. Evidence: `artifacts/camera-failure-smoke.json`. The test first tried a graceful FFmpeg signal, which did not terminate promptly; the verified run used an abrupt crash of only its exact test-owned publisher.
- Restart recovery marks unfinished takes interrupted and renders failed, expires queued directions and process-local hold/override state, and preserves completed edits and source media. Ten Python tests and production build pass after these changes.
- Post-recovery browser regressions: Chrome two passed in 10.8 seconds (paid voice test intentionally skipped); WebKit/iPhone emulation two passed in 11.3 seconds. Earlier paid voice and AI runs remain separately documented above.
- Added exact decoded-source-frame verification: known-offset, lost-frame, drift, ambiguous-frame and requested-range checks. The cinematic take had 963 matching frames per camera and contiguous segment coverage. This proves media identity, not live-cut wall-clock alignment. See `docs/MEDIA_VERIFICATION.md`.
- Real HTTP → recordings → frame matching → render → authenticated source-manifest export passed; subsequent Chrome review/mobile tests both passed in 10.0 seconds. The review visibly labels live timing as estimated and exposes Source evidence. Thirteen Python tests and the production build pass.
- All-camera crash test passed: the watchdog automatically stopped the take, kept its state PARTIAL and preserved media. Evidence: `artifacts/all-camera-failure-smoke.json`. No publishers or renders remained active after this test.
- API admission now supports a non-persisted invitation code, session-scoped production keys, exact browser-origin checks for HTTP/WebSockets and no-store responses. Public-origin configuration requires a strong invitation. This does not yet secure MediaMTX's separate ports. See `docs/ACCESS_AND_LIMITS.md`.
- Added atomic UTC-day usage reservations in `data/usage.sqlite` for production creation, takes, renders, AI jobs, speech duration and voice requests. Limits survive restarts; identical operations are idempotent; concurrent handles cannot overspend. These are operation caps, not a dollar cap. CUT remains available after allowance exhaustion.
- Isolated zero-AI-budget access smoke passed: invitation/session isolation, foreign HTTP/WebSocket origins, session cap, denied AI/voice work, real CUT at exhausted take allowance, rejected next roll. Evidence: `artifacts/access-smoke.json`. No Google call was made by that smoke.
- Invitation UI passed wrong-code rejection and successful entry without persisting the invitation. An actual API outage/retry preserved production `e3c76a91ac4b4f639f8d43c1388690df`; transient errors no longer discard the saved production. Temporary test admission was removed afterward.
- Sixteen Python tests and production build pass. With new usage controls enabled, real Chrome media/AI-edit/mobile/voice regressions: all three passed in 50.7 seconds.
- Post-admission WebKit/iPhone-emulation regressions both passed in 12.5 seconds. These remain browser-engine checks, not physical-device validation.
- User approved hosting up to $25 using the linked billing account, then explicitly instructed development to proceed without further credit checks or BrowserOS. Dedicated VM `clappy-demo-1` is now running with a verified 24-hour automatic STOP; trusted HTTPS, invitation admission, private control/database ports, attached service-account ADC, project-only gross budget alerts, and a persistent egress guard are configured. Credit eligibility is not independently certified. See `docs/HOSTING.md` for storage costs after STOP and operational limits.
- Scoped MediaMTX authentication is implemented: private API-only administrator, six exact-path/action accounts per take, production-authenticated player/ticket endpoints, redacted publisher logs and normal CUT revocation. The masked config-GET password issue is covered by a regression. See `docs/MEDIA_ACCESS.md` for restart and host-trust limitations.
- Media-auth smoke passed scoped access and post-CUT revocation checks. Authenticated recording/Chrome playback passed; the subsequent WebKit run reports no failed tests. Eighteen Python tests pass and the locked Python environment is synchronized.
- Protected RTSP → actual Google transcription → ADK/ClickHouse MCP regression passed: all six lines, four memory-backed live decisions, manual hold/release, 38.2-second take. No alternate provider or canned AI output was substituted.
- Post-media-auth recovery regressions pass: selected-camera loss falls back to C and releases an impossible hold; all-camera loss automatically stops with PARTIAL coverage. Isolated admission/usage smoke also passes, including CUT after budget exhaustion, with zero Google calls.
- Added bounded, session-authenticated client composition observations with per-take/player epochs, deduplication, stale-decision checks and separate server receipt times. The source evidence manifest retains these independently of source-frame alignment; the original edit still honestly labels live timing as estimated.
- Chrome browser-only paused-player test passed: C → A fallback while all three publishers remained RECORDING, followed by A composition acknowledgment. Take `2883f6eb85ca4b17acbcb506dafe10fe` reached its 50-second auto-stop, verified all recordings and completed review. WebKit's constant-zero media timestamps were observed and frame health now uses compositor-frame counts instead.
- Clean extended WebKit check passed after startup/visibility hardening: healthy playback without false stalls, C paused only in its program player, A fallback after 2513 ms, and separate A acknowledgment while all publishers stayed RECORDING. Take `ebc4340a21be45eb9b73e91a81ad8ad0`; screenshot `output/playwright/webkit-monitor-fallback-verified.png`. Development hot reload now reuses the React root. Twenty-three Python tests pass.
- That WebKit take finalized at 10.333 seconds with verified recordings and a READY MP4. Its source manifest preserves all three monitor observations and the matching take clock epoch. Post-observer Chrome regressions: two passed in 11.3 seconds; WebKit/iPhone-emulation regressions: two passed in 13.8 seconds. Production build passes. Live-source clock calibration remains unproven.

## Next work

### Hosted milestone — September 7 evening

- Trusted HTTPS deployment at `https://35-254-38-201.sslip.io`; fresh invitation rejection/acceptance verified. Six actual Chrome WebRTC subscriptions decoded 960-pixel video. Source audio reached Google Speech using attached service-account ADC. No developer token was copied to the VM.
- Real hosted original + Gemini/official-MCP alternate passed on take `1e57ff78719e4b56a8a3e6fe3e9b199d`. Both 27.133-second versions remain READY; alternate `1838c521c6084262ab47828280d38c9d` is the explicitly requested 100% Bella version. Both downloaded video hashes match their manifests. A paused five-second playhead survived version switching, and the alternate actually played.
- Initial hosted live requests were too slow and were correctly rejected. Bounded creative context now excludes raw clock telemetry; mandatory live MCP queries run over one connection per take before a single Google inference. The connection closes in its owning policy task at CUT. Full edits retain the ADK tool loop. Live uses Flash-Lite; full edits use Flash.
- Hosted WebKit at 390×844 passed no-horizontal-overflow and real WebRTC decoding. Take `3e0bc5ba162045f4ab88ec454683909e` finalized with verified recordings and READY render, two compositor observations and 24 source-code batches. Two live choices applied at 0.878/0.914 seconds. Earlier overlong explanations were rejected; an ADK output-schema integration regression now prevents that configuration mistake.
- Final structured-live take `8207814dacf64f629803038223820b94`: two Google choices applied at 0.942/0.914 seconds with official MCP evidence. Cold first request took 6.329 seconds and was rejected as stale; do not claim sub-second cold-start latency. No failed-format event occurred in that final run.
- Production build and **38 Python tests pass**; real ClickHouse/MCP scope/read-only/deduplication smoke passes with telemetry excluded. Single-rig claim is now synchronous before its first await, preventing two productions racing to start publishers.
- Evidence: `artifacts/hosting/smoke-summary.json`; screenshots in `output/playwright/hosted-alternate.png`, `hosted-webkit-live.png`, `hosted-live-verified.png`. `hosted-walkthrough.webm` is an 84.6-second **QA recording from an earlier run**, not the finished narrated submission demo and not proof of successful autonomous direction in that earlier run.
- Hosted controls include invitation, scoped media accounts, loopback control/database ports, HTTPS, nginx request/body/connection bounds, eight WebSockets per production, daily allowances, 4 GiB disk reserve, 10 GiB egress circuit breaker, gross $25 project budget alerts and automatic VM STOP after 24 hours. Persistent disk charges continue after STOP. See `HOSTING.md`.
- Hosted voice API passed roll, hold Bella, wide and cut with actual Google recognition/intent handling and deduplicated requests. Server processing was approximately 1.7–2.3 seconds. Evidence: `artifacts/hosting/voice-api.json`.
- Hosted browser MediaRecorder → upload → Google recognition → real roll/CUT passed with an explicitly injected, owned WAV-backed MediaStream. Screenshot: `output/playwright/hosted-voice-roll.png`. Chrome's CLI fake-device input produced zero measured signal, so that mechanism was not counted as passing; physical microphone capture remains unverified. No app response or server result was mocked.
- **40 Python tests and the production build pass.** The deployed egress guard now also fails closed when route discovery fails and attempts media shutdown even if web-service shutdown fails. All three hosted service checks remained active after installing the fix.
- Installed Python/npm attribution inventory generated: 260 packages and 305 supplied notice/license files under `artifacts/notices/`. This is not OS/container redistribution-compliance certification; see `THIRD_PARTY_NOTICES.md`.
- Final walkthrough take `84259309749f4e2688065d6a508b8842` passed: 29.333 seconds, five recognized dialogue lines, three accepted memory-backed Google proposals (0.804/0.977/0.966 seconds; repeated Bella choices do not invent extra cuts), one actual C→B edit boundary, manual Bella hold, three verified recordings, READY original and READY 100% Bella alternate. Both exported video hashes match manifests. A/B preserved the five-second paused playhead and subsequent playback; final observed playhead 13.357 seconds.
- `output/playwright/clappy-final-walkthrough.webm` is the **169.28-second actual hosted UI recording** of that final manual-roll/live-AI/hold/CUT/alternate/review flow. It is silent, unaccelerated, includes waiting time, and does not demonstrate physical voice input. Browser synthetic voice evidence is separate. `DEMO_RUNBOOK.md` supplies the demonstration narration and operational cautions.

Source-clock update: new takes now use immutable frame-coded derivatives; all 16,200 frames in nine prepared source files passed sequence/CRC verification. Browser pixel reads are tied to bounded clock exchanges and stored separately from compositor acknowledgments. Chrome produced 48 live batches (one camera fit correctly rejected as too uncertain); WebKit produced 14 batches from all three cameras and decoded source frame 240 for the manual A cut despite browser PTS zero. WebKit take `8caff6d3c43143e892334617b9373127` rendered with verified recordings and retained calibration evidence. Thirty Python tests and the production build pass. See `docs/SOURCE_CLOCK.md`; live-edit retiming remains unverified.

Post-source-version regressions also pass: actual Google transcription recognized all six lines, ADK/ClickHouse applied five memory-backed live decisions, and manual hold/release worked during a 38.3-second take. Chrome media/review/mobile: two passed in 28.8 seconds; WebKit/iPhone emulation: two passed in 12.2 seconds.

1. Harden speech/AI recovery and stale/queued control cases. Current speech/AI tests use original synthetic dialogue, not a physical shoot.
2. Measured session-time synchronization. Exact recorded-source-frame matching is implemented, but the live command's source-time origin remains a publisher-clock estimate. Test delayed publishers, client-applied timing and recovery.
3. Finish the recorded visual walkthrough; cinematic source, A/B playhead preservation, allocation comparison, and master sound now have local evidence. Retain the honest still-frame/derived-view disclosure.
4. Extend failure coverage to delayed starts, background tabs, multi-director views and recovery. Browser-only stalled-playback observations/fallback and single-/all-camera process loss now have local evidence; destination subscription health is checked separately from phone previews. No native app, Apple Simulator, or physical-device claims.
5. Hosted manual media, live Google direction, Google/MCP alternate edit, A/B playback, synthetic browser voice and WebKit checks now have external evidence. Longer recovery coverage and physical microphone validation remain distinct from these passes. No further credit check is requested.
6. Finish reproducible runbook, notices/provenance, visual demo and submission materials.

## Completion evidence still missing

M0/M1/M3/M4/M5/M6 still have unmet exit criteria in the larger implementation plan, especially measured live-edit retiming and broader recovery testing. Working media and Google AI flows are real, but are not full-product acceptance. No Devpost submission has been created. At the user's request, the existing implementation is now being imported into Git as focused component commits for `Shiv-aurora/google-agentic-cinema-hackathon`; these are retrospective imports, not a reconstruction of the original development chronology.
